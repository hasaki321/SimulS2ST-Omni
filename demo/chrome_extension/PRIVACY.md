# Privacy

The unpacked extension captures audio only from the tab selected by the user and
sends PCM audio to the WebSocket URL configured by the user. It does not include
analytics, advertising, telemetry, or a third-party backend.

The broad page permission is used to inject the optional caption overlay into
the active page. The extension source is included in this repository for audit.
Only connect it to a server you trust.
